---
- hosts: all
  become: true
  tasks:
    - name: install epel-release
      package:
        name: epel-release
        state: present
    
    - name: install screen
      package:
        name: screen
        state: present
      
    - name: install stress-ng
      package:
        name: stress-ng
        state: present

    - name: install collectd
      package:
        name: collectd
        state: present

 

